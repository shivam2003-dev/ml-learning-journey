[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "tiny-cnn"
version = "0.1.0"
dependencies = ["numpy>=1.24"]

[project.scripts]
tiny-cnn = "tiny_cnn.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
