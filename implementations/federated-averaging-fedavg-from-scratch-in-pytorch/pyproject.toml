[project]
name = "federated-averaging-fedavg-from-scratch-in-py"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["numpy", "torch; platform_machine != 'none'", "jax; platform_machine != 'none'"]

[tool.pytest.ini_options]
pythonpath = ["."]
